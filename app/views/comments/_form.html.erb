<%# This doesnt work. Fix form for nested resource %>
<%= simple_form_with([@article, @article.comments.build]) do |form| %>
  <div class="field">
    <div class="control">
      <%= form.input :body, input_html: {class: 'textarea'}, wrapper: false, label_html: {class: 'label'} %>
    </div>
    <div class="control">
      <%= form.input :commenter, wrapper: false, :as => :hidden, :input_html => { :value => current_user.username } %>
    </div>
  </div>
  <%= form.button :submit, class: "button is-link" %>
<% end %>
